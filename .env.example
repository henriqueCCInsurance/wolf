# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# OAuth Configuration Notes:
# To enable Google OAuth:
# 1. Go to Supabase Dashboard > Authentication > Providers
# 2. Enable Google provider
# 3. Add your Google OAuth credentials from Google Cloud Console
# 4. Set redirect URL in Google Cloud Console to: https://yourproject.supabase.co/auth/v1/callback
# 5. Add your domain to Supabase Auth > URL Configuration > Site URL

# To enable GitHub OAuth:
# 1. Go to Supabase Dashboard > Authentication > Providers
# 2. Enable GitHub provider
# 3. Create OAuth App in GitHub Settings > Developer settings > OAuth Apps
# 4. Set Authorization callback URL to: https://yourproject.supabase.co/auth/v1/callback
# 5. Add Client ID and Client Secret to Supabase

# To enable Apple Sign In:
# 1. Go to Supabase Dashboard > Authentication > Providers
# 2. Enable Apple provider
# 3. Register an App ID in Apple Developer Console
# 4. Create a Service ID for web authentication
# 5. Configure Return URLs to: https://yourproject.supabase.co/auth/v1/callback
# 6. Generate a private key and add to Supabase along with your Service ID
# Note: Apple Sign In requires a paid Apple Developer account

# Local Development:
# Add http://localhost:5173 to Supabase Auth > URL Configuration > Redirect URLs